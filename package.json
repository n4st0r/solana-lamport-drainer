{
  "name": "wallet-drain",
  "version": "1.0.0",
  "main": "./dist/index.js",
  "license": "MIT",
  "scripts": {
    "build": "yarn clean && yarn build:bin",
    "build:bin": "tsc",
    "clean": "yarn rimraf dist",
    "start": "node ./dist/index.js",
    "all": "yarn build && yarn start"
  },
  "devDependencies": {
    "@types/node": "^16.11.12",
    "rimraf": "^3.0.2",
    "typescript": "^4.5.3"
  },
  "dependencies": {
    "@solana/web3.js": "^1.31.1",
    "dotenv": "^10.0.0",
    "rxjs": "^7.4.0",
    "winston": "^3.3.3"
  }
}
